# 上下文切换
### 1. 切什么？—— 理解“CPU 上下文”

在 CPU 看来，它不知道什么是“进程”，它只知道当前寄存器里存了什么数据。所谓的“上下文（Context）”，就是**保证进程能够从中断点继续运行所需的一整套 CPU 状态集合**。

在 408 考点中，上下文主要包含两类信息：

1. **用户级上下文（内存资源）**：
    
    - 程序的指令（代码段）、数据（数据段）。
        
    - 用户栈（User Stack）：存局部变量、函数调用链。
        
    - **关键点**：这部分通过**虚拟内存映射**管理，切换时主要涉及页表指针的切换。
        
2. **系统级上下文（寄存器状态 - 最关键）**：
    
    - **PC (Program Counter)**：下一条要执行指令的地址（断点）。
        
    - **SP (Stack Pointer)**：指向当前的栈顶（可能是内核栈）。
        
    - **PSW (Program Status Word)**：程序状态字，记录算术运算结果（进位、溢出）和当前模式（用户态/核心态）。
        
    - **通用寄存器**：AX, BX, CX 等计算用的临时数据。
        
    - **CR3 (页表基址寄存器)**：指向当前进程页表的物理地址（这点至关重要，决定了看哪张内存地图）。
        

---

### 2. 怎么切？—— 上下文切换的详细流程

这是一个“暂停 A，启动 B”的过程。假设此时 **进程 A** 正在运行，调度程序决定切换到 **进程 B**。

这个过程完全在 **核心态** 下执行，通常由汇编语言编写以追求极速。

#### 第一阶段：保存现场 (Save Context of A)

1. **中断/陷阱触发**：CPU 从用户态陷入内核态。
    
2. **保存硬件状态**：
    
    - 硬件自动（或内核软件）将进程 A 的 PC、PSW 压入 **进程 A 的内核栈**。
        
    - 上下文切换器将 A 的通用寄存器、SP 等推入 **进程 A 的 PCB** 中（或者内核栈中）。
        
3. **更新 PCB**：
    
    - 修改 A 的状态：运行态 $\rightarrow$ 就绪态/阻塞态。
        
    - 将 A 的 PCB 移入相应的队列（由排队器完成）。
        

#### 第二阶段：调度决策 (Scheduler Decision)

- 调度器（算法）选定了 **进程 B**。
    
- 分派器拿到进程 B 的 PCB 指针。
    

#### 第三阶段：恢复现场 (Restore Context of B)

1. **切换内存空间（最耗时）**：
    
    - 将 **进程 B 的页目录基址**（Page Directory Pointer）加载到 **CR3 寄存器**。
        
    - _后果_：这会告诉 MMU（内存管理单元）：“以后查地址请看 B 的地图”。**这通常会导致 TLB（快表）被刷新/清空**。
        
2. **加载硬件状态**：
    
    - 从 **进程 B 的 PCB**（或内核栈）中弹出之前保存的通用寄存器数据，填回 CPU 寄存器。
        
    - 恢复 B 的 SP（栈指针）。
        
3. **更新 PCB**：
    
    - 修改 B 的状态：就绪态 $\rightarrow$ 运行态。
        
4. **执行流跳转**：
    
    - 执行 `IRET`（中断返回）类指令。
        
    - CPU 将 B 的 PC 和 PSW 弹回寄存器。
        
    - **此刻，CPU 开始执行进程 B 的代码**。
        

---

### 3. 代价是什么？—— 为什么它是“纯开销”？

上下文切换竟然是计算密集型的，有的cpu会提供多个寄存器组，上下文切换就可以只简单改变当前寄存器组的指针

在 408 大题中，如果问你“为什么时间片设置得太小会导致系统效率下降”，答案就在这里。

上下文切换不仅仅是“复制数据”那么简单，它有隐形的性能杀手：

1. **显式开销（CPU 时间）**：
    
    - 保存和恢复几十个寄存器需要指令周期。
        
    - 内核代码本身的执行时间。
        
2. **隐式开销（TLB 与 Cache 失效 - 杀手级）**：
    
    - **TLB 刷新**：当你切换 CR3（页表）时，TLB（快表）里缓存的 A 进程的地址映射全都没用了，必须清空。
        
    - **Cache 污染**：进程 B 刚跑起来，CPU 的 L1/L2 Cache 里全是 A 的数据（脏数据）。B 需要从慢速内存重新加载数据，导致切换后的最初一段时间执行速度变慢。
        

---

### 4. 易混淆点辨析（408 挖坑点）

#### Q1: 模式切换（Mode Switch） vs. 上下文切换（Context Switch）

- **模式切换**：用户态 $\leftrightarrow$ 核心态。
    
    - _发生_：系统调用时。
        
    - _开销_：小。只需要保存很少的寄存器，不换页表。
        
- **上下文切换**：进程 A $\leftrightarrow$ 进程 B。
    
    - _发生_：调度时。
        
    - _开销_：大。要换页表，动全身。
        
- **结论**：上下文切换**一定**伴随着模式切换；但模式切换**不一定**导致上下文切换。
    

#### Q2: 为什么线程切换快？

- 因为同一个进程内的线程，共享同一个页表（CR3 不变）。
    
- **不需要刷新 TLB**，也不完全需要刷新 Cache。这是线程切换比进程切换快的主要原因。
    

---

### 💡 总结复习卡片

对于“上下文切换”的实现，请记住以下核心逻辑链：

> **中断挂起 $\rightarrow$ 存旧 PCB/栈 $\rightarrow$ 选新进程 $\rightarrow$ 换页表 (CR3) $\rightarrow$ 刷新 TLB $\rightarrow$ 载新 PCB/栈 $\rightarrow$ 跳转执行**

# 进程切换
## 一、 核心骨架：线程切换的通用流程

无论进程还是线程，切换的本质是**控制权（CPU 寄存器状态）的转移**。由于现代操作系统通常以线程作为调度单位，我们先看最基础的线程切换（同进程内）。

### 1. 触发陷阱（Trap/Interrupt）

切换通常始于时钟中断或系统调用。此时 CPU 从用户态切换到内核态。

- **硬件动作**：CPU 自动将当前的程序计数器 ($PC$)、堆栈指针 ($SS/SP$)、标志寄存器 ($EFLAGS$) 压入该线程的**内核栈**（Kernel Stack）。
    

### 2. 保存上下文（Context Save）

内核代码（调度器）开始执行，将剩余的通用寄存器（如 $EAX, EBX, ESI$ 等）压入内核栈。

- **TCB 更新**：将当前线程内核栈的栈顶指针保存到其 **线程控制块（TCB）** 中。至此，该线程被“封冻”。
    

### 3. 执行调度算法

内核根据策略（如 CFS）从就绪队列中选出下一个要运行的线程。

### 4. 恢复上下文（Context Restore）

- **切换内核栈**：将 CPU 的堆栈指针寄存器 $SP$ 指向新线程的内核栈顶。
    
- **出栈**：从新线程的内核栈中弹出之前保存的通用寄存器。
    
- **中断返回（iret/sysret）**：执行特殊的返回指令，硬件自动从内核栈弹出 $PC$、$EFLAGS$ 等，CPU 回到用户态，新线程开始执行。
    

---

## 二、 进阶差异：进程切换的“沉重”之处

如果新旧线程属于**不同的进程**，那么除了上述流程外，还必须进行**地址空间的切换**。这是进程切换比线程切换慢得多的根本原因。

### 1. 修改页表基址寄存器（CR3 寄存器）

每个进程都有独立的虚拟地址空间。

- **动作**：内核会将新进程页目录的物理地址加载到 CPU 的 **$CR3$ 寄存器**（在 x86 架构下）。
    
- **后果**：这一步之后，CPU 翻译同一个虚拟地址（如 `0x4000`）时，会指向完全不同的物理内存。
    

### 2. 内存映射的变化

虽然内核空间在所有进程中是共享的（为了提高效率），但用户空间（代码段、数据段、堆栈）被完全替换。这意味着旧进程的所有内存缓存映射在逻辑上都失效了。

---

## 三、 硬件层的深度细节：看不见的性能损耗

这是 408 考研的高级考点，也是架构师必须具备的“直觉”。

### 1. TLB（快表）的失效：最直接的重击

TLB 存储了虚拟地址到物理地址的映射。

- **进程切换时**：由于页表变了，旧的 TLB 条目全错了。传统做法是**冲刷（Flush）整个 TLB**。
    
- **副作用**：切换后的前几千个指令会遭遇大量的 **TLB Miss**，CPU 必须多次访问物理内存来查页表，流水线频繁停顿。
    
- **优化（ASID）**：现代 CPU 支持进程标识符（Address Space ID），允许 TLB 缓存保留不同进程的条目，从而减轻这个代价。
    

### 2. 高速缓存（Cache）的污染与冷启动

CPU 的 L1/L2/L3 Cache 是按地址或标签缓存数据的。

- **Cache Pollution**：虽然 Cache 不一定在切换时被物理清空，但新进程访问的数据与旧进程完全不同。旧进程的数据会被逐渐换出（Evict）。
    
- **冷启动（Cold Start）**：新进程运行初期，Cache 命中率极低，这种现象被称为“缓存冷启动”。
    

### 3. 分支预测器（Branch Predictor）失效

CPU 内部的 BTB（Branch Target Buffer）记录了历史跳转分支。进程切换后，新代码的跳转逻辑与旧代码完全不同，导致分支预测失败率激增，进一步拖慢流水线。

---

## 四、 知识直觉：形象化对比

为了帮你建立直觉，我们可以用“办公室切换”做类比：

- **线程切换**：你在同一个办公室（进程），只是换了一个任务（线程）。
    
    - **代价**：放下手头的笔，拿起另一份报告。桌子还是那张桌子，工具（内存/Cache）都在手边。
        
- **进程切换**：你被要求去另一个大楼的办公室办公。
    
    - **代价**：你得带上自己的私人物品（寄存器），并且当你到了新办公室，你需要重新找饮水机在哪里、厕所在哪里（TLB/Cache 失效）。你需要一段“适应期”才能达到之前的办事效率。
        

---

## 五、 408 考研核心总结表

|**特性**|**线程切换（同进程）**|**进程切换**|
|---|---|---|
|**内核参与**|是|是|
|**保存/恢复寄存器**|是|是|
|**切换虚拟地址空间**|**否**|**是（修改 $CR3$）**|
|**TLB 刷新**|否|是（除非有 ASID）|
|**Cache 效率**|高（热启动）|低（冷启动/污染）|
|**开销级别**|轻量级|重量级|
![[线程进程切换.png]]
## 关于switch_to
### 1. 它在什么位置？（调用栈）

当你调用 `sleep()` 或者时钟中断发生时，内核会走这样一条路：

1. `schedule()`：调度主函数，决定谁是下一个（Next）。
    
2. `context_switch()`：上下文切换入口。
    
3. **`switch_to(prev, next)`**：**这就是终极的大招。** 它直接操作汇编指令，更换堆栈。
    

---

### 2. 它的核心黑科技：交换堆栈（Stack Switch）

这是最神奇的地方。每个进程（线程）都有自己的**内核栈**。`switch_to` 的核心任务就是把 CPU 的 $ESP$（或 $RSP$）寄存器从“旧进程的内核栈”挪到“新进程的内核栈”。

### 具体的汇编级动作（以 x86 为例）：

- **保存当前：** 把旧进程 A 的所有寄存器（$EBP, EBX, ESI$ 等）压入 A 的内核栈。
    
- **切换指针：** 把 CPU 的 $ESP$ 寄存器的值改掉，指向新进程 B 的内核栈。
    
    > **直觉时刻：** 这一步一做完，CPU 就已经认为它在运行进程 B 了！因为现在的栈顶指针在 B 的地盘上。
    
- **恢复新程：** 从 B 的内核栈里弹出之前保存的寄存器。
    
- **跳回执行：** 弹出 B 之前保存的 $EIP$（指令指针），CPU 瞬间跳到 B 上次被切换走的地方继续执行。
    

---

### 3. 为什么这个东西对你“很重要”？

虽然 408 不会让你默写 Linux 的 `switch_to` 源码，但理解它能帮你秒杀以下考点：

#### A. 进程切换开销到底在哪？

通过 `switch_to` 你会发现，切换不仅仅是改个寄存器，它涉及**两次内存读写**（存栈和取栈）。这解释了为什么频繁切换会拖慢系统速度。

#### B. 为什么内核栈是独立的？

如果所有进程共用一个内核栈，`switch_to` 切换 $ESP$ 的时候，整个程序的执行逻辑就全乱了。**内核栈是进程私有的，这是实现并发的基础。**

#### C. “由于进程切换发生在内核态”

这句考研常考的话，现在有了具体的图景：`switch_to` 里面的汇编指令（如修改 $CR3$ 寄存器或 $ESP$）都是**特权指令**，必须在内核态执行。

---

### 4. 一个让你起鸡皮疙瘩的细节：`last` 参数

在 Linux 源码里，这个宏其实有三个参数：`switch_to(prev, next, last)`。

- **谜题**：进程 A 切换到 B，再从 C 切换回 A。当 A 重新醒来时，它怎么知道是谁把它换下来的（即 C 是谁）？
    
- **答案**：`switch_to` 利用 CPU 寄存器的残留值，通过 `last` 参数把“前任”的信息传给“现任”。这体现了底层编程中极其精妙的逻辑闭环。

# linux调度器详解
### 1、调度器

**1.1、概念**

在Linux内核中，调度器（Scheduler）是操作系统内核的关键组件之一，负责管理和协调CPU资源在多个进程间高效、公正地分配。调度器的主要目标是确保CPU时间片能在系统中的各个进程间合理分配，从而最大化系统资源利用率，同时也要考虑进程的响应时间和整体系统性能。

**1.2、结构框图**

Linux内核中用来安排调度进程 (一段程序的执行过程) 执行的模块称为调度器(Scheduler)，它可以切换进程状态 (Process status) 。比如: 执行、可中断睡眠、不可中断睡眠、退出、暂停等。

![](https://pic4.zhimg.com/v2-d8cec8e4001f565d1172e810a2db9523_1440w.jpg)

调度器是 CPU 中央处理器的管理员，主要负责完成做两件事情：

一、选择某些就绪进 程来执行。

二、是打断某些执行的进程让它们变为就绪状态。

调度器分配CPU 时间的基本依据：进程的优先级。

上下文 切换（context switch ）：将进程在 CPU 中切换执行的过程，内核承担此任务，负责重建和存储被切换掉之前的CPU 状态。

**1.3、Linux下的调度类介绍（sched_class）**

①、sched_class数据结构介绍

调度类是内核调度框架中更为抽象的概念，它是一种组织和管理调度策略的结构化方式。在Linux内核中，每个调度类都对应一个具体的调度策略，或者一套相关的调度策略，并提供了一系列与调度相关的函数接口，如进程入队、出队、选择下一个运行进程等操作。

sched_class结构体是在代码中体现调度类，描述了调度器的基本操作接口，内核根据不同调度策略（如CFS、实时调度等）实现了对应的调度类，这些调度类则通过实现下面的接口来参与到内核的整体调度流程中。

数据结构定义在kernel/sched/sched.h

```c
struct sched_class {
 
#ifdef CONFIG_UCLAMP_TASK
	int uclamp_enabled;
#endif
	/* 将进程加入到执行队列当中，即将调度实体(进程)存放到红黑树中，并对nr_running变量自动会加1*/
	void (*enqueue_task) (struct rq *rq, struct task_struct *p, int flags);
 
	/* 从执行队列当中删除进程，并对nr_running变量自动减1 */
	void (*dequeue_task) (struct rq *rq, struct task_struct *p, int flags);
	
	/*放弃CPU执行权，实际上该函数执行先出队后入队，在这种情况下，它直接将调度实体放在红黑树的最右端 */
	void (*yield_task)   (struct rq *rq);
	bool (*yield_to_task)(struct rq *rq, struct task_struct *p);
	
	/* 用于检查当前进程是否可被新进程抢占 */
	void (*check_preempt_curr)(struct rq *rq, struct task_struct *p, int flags);
	
	/* 选择下一个应用要运行的进程*/
	struct task_struct *(*pick_next_task)(struct rq *rq);
	
	/*将进程放回到运行队列当中 */
	void (*put_prev_task)(struct rq *rq, struct task_struct *p);
	void (*set_next_task)(struct rq *rq, struct task_struct *p, bool first);
 
#ifdef CONFIG_SMP
	int (*balance)(struct rq *rq, struct task_struct *prev, struct rq_flags *rf);
 
	/* 为进程选择一个合适的CPU */
	int  (*select_task_rq)(struct task_struct *p, int task_cpu, int flags);
	struct task_struct * (*pick_task)(struct rq *rq);
	
	/* 迁移任务到另一个CPU */
	void (*migrate_task_rq)(struct task_struct *p, int new_cpu);
	
	/*专门用于唤醒进程 */
	void (*task_woken)(struct rq *this_rq, struct task_struct *task);
	
	/* 修改进程在CPU的亲和力 */
	void (*set_cpus_allowed)(struct task_struct *p,
				 const struct cpumask *newmask,
				 u32 flags);
	
	/* 启动运行队列 */
	void (*rq_online)(struct rq *rq);
	
	/* 禁止运行队列 */
	void (*rq_offline)(struct rq *rq);
	struct rq *(*find_lock_rq)(struct task_struct *p, struct rq *rq);
#endif
	/* 调用自time_tick函数，它可能引起进程切换，将驱动运行时(running)抢占 */
	void (*task_tick)(struct rq *rq, struct task_struct *p, int queued);
 
	/* 当进程创建的时候调用、不同调度策略的进程初始化也是不一样的*/
	void (*task_fork)(struct task_struct *p);
	
	/* 进程退出的时候使用*/
	void (*task_dead)(struct task_struct *p);
 
	/*
	 * The switched_from() call is allowed to drop rq->lock, therefore we
	 * cannot assume the switched_from/switched_to pair is serialized by
	 * rq->lock. They are however serialized by p->pi_lock.
	 */
	/* 专门用于进程的切换*/
	void (*switched_from)(struct rq *this_rq, struct task_struct *task);
	void (*switched_to)  (struct rq *this_rq, struct task_struct *task);
	
	/*进程优先级的更改 */
	void (*prio_changed) (struct rq *this_rq, struct task_struct *task,
			      int oldprio);
	unsigned int (*get_rr_interval)(struct rq *rq,
					struct task_struct *task);
	void (*update_curr)(struct rq *rq);
 
#ifdef CONFIG_FAIR_GROUP_SCHED
	/* */
	void (*task_change_group)(struct task_struct *p);
#endif
};
```

比较重要的几个成员接口：

- enqueue_task: 向就绪队列添加一个进程，某个任务进入可运行状态时，该函数将会调用，它将调度实体放入到红黑树当中。
- dequeue_task: 将一个进程从就绪队列中进行删除，当某个任务退出可运行状态时调用该函数，将从红黑树中去掉对应调度实体。
- yield_task:在进程想要资源放弃对处理器的控制权时，可使用在sched_yiled系统调用，会调用内核API去处理操作。
- check_preempt_curr: 检查当前运行的任务是否被抢占。
- pick_next_task: 选择下来要运行的最合适的实体 (进程)。
- put_prev_task: 用于另一个进程代替当前运行的进程。
- set_curr_task: 当任务修改它调用类或修改它的任务组时，将调用这个函数。
- task_tick: 在每次激活周期调度器时，由周期性调度器调用。

②、Linux下的五大调度类

Linux内核中实例化了五个调度类，代码如下：

```c
extern const struct sched_class stop_sched_class;
extern const struct sched_class dl_sched_class;
extern const struct sched_class rt_sched_class;
extern const struct sched_class fair_sched_class;
extern const struct sched_class idle_sched_class;
```

- stop_sched_class 停机调度类: 优先级是最高的调度类，停机进程是优先级最高的进程，可以抢占所有其它进程，其他进程不可能抢占停机进程。
- dl_sched_class 指Deadline调度类，它指的是具有固定截止期限（deadline）的调度策略，这类策略适用于对完成时间有严格限制的任务
- rt_sched_class 表示实时调度类（Real-Time Scheduling Class），这是用于实时进程调度的策略，包括SCHED_FIFO（先进先出）和SCHED_RR（循环轮转）两种调度策略。实时进程在调度时可以获得更高的优先级，并能抢占其他非实时进程以保证及时响应。
- fair_sched_class 即完全公平调度类（Completely Fair Scheduler, CFS），它是Linux内核自2.6.23以来用于普通进程的标准调度策略。CFS设计的目标是在所有可运行进程之间公平地分配CPU时间，基于进程的动态优先级（由nice值和CPU使用时间等因素决定）进行调度。
- idle_sched_class 是空闲调度类，它负责调度idle任务，即idle进程。当系统的所有其他进程都不需要CPU时，idle进程会被调度执行，通常它的作用是降低CPU利用率，避免无谓的忙碌等待，并在必要时触发电源管理相关的操作。

③、调度器结构分析

![](https://picx.zhimg.com/v2-dec9865091c262df16899a6384eb623d_1440w.jpg)

当处理器空闲时，它会向主调度器请求分配新任务，主调度器依据任务列表及其优先级等信息作出决策，通过上下文切换将CPU使用权转交给合适的任务。此外，周期性调度器定期唤醒主调度器以重新评估系统负载，并据此可能调整任务执行计划，如在高负载时减少低优先级任务执行，确保关键服务获得足够资源。

**1.5、调度优先级**

task_struct结构体中采用三个成员表示进程的优先级： prio 和 normal_prio 表示动态优先级 ,static_prio 表示进程的静态优先级。

内核将任务优先级划分，实时优先级范围是0 到 MAX_RT_PRIO-1 （即 99 ），而普通进

程的静态优先级范围是从 MAX_RT_PRIO 到 MAX_PRIO-1 （即 100到139）。 代码定义在 Linux 内核源码： /include/linux/sched/prio.h 。

![](https://pic2.zhimg.com/v2-9d474a1a07dbe3994c963698ce2fbd43_1440w.jpg)

实时进程与普通进程有着明确的优先级区间：实时进程的优先级范围是0至99，这类进程往往对应于对响应时间和执行效率要求极高的应用，比如音频视频流媒体播放、工业控制等领域。数字越小的实时进程享有更高的优先级，因此在争夺CPU资源时能优先得到执行。

另一方面，普通进程的优先级设定在100至139之间，这类进程主要包括日常的非实时应用，如Web服务器、数据库管理系统等。同样遵循数值越小优先级越高的原则，即使在普通进程中，较小优先级的进程也会相对优先被执行。

**1.6、调度策略**

Linux内核提供了一些调度策略用来选择调度器

linux 内核调度策略源码： /include/uapi/linux/sched.h 。代码定义如下：

```c
/*
 * Scheduling policies
 */
#define SCHED_NORMAL		0
#define SCHED_FIFO		1
#define SCHED_RR		2
#define SCHED_BATCH		3
/* SCHED_ISO: reserved but not implemented yet */
#define SCHED_IDLE		5
#define SCHED_DEADLINE		6
```

SCHED_NORMAL：

定义了正常进程调度策略，即非实时调度策略。在Linux内核中，使用完全公平调度器（Completely Fair Scheduler, CFS）来实现，它旨在为所有进程提供公平的CPU时间分配，依据进程的nice值（优先级）和虚拟运行时间来平衡各进程的执行机会。

SCHED_FIFO：

代表先进先出（First In, First Out）的实时调度策略。在该策略下，实时进程按到达就绪队列的顺序执行，具有该调度策略的进程一旦获得CPU就不会被更低优先级的进程抢占，除非自身主动释放CPU，或者有更高优先级的SCHED_FIFO或SCHED_RR实时进程变得可运行。

SCHED_RR：

轮转（Round Robin）实时调度策略，类似于SCHED_FIFO，但是实时进程在一个时间片（quantum）结束之后会被放置到同优先级实时进程队列的末尾，等待再次轮到自己执行，这样保证同一优先级的实时进程可以轮流获得执行机会。

SCHED_BATCH：

批处理调度策略，适用于CPU密集型且对响应时间要求不高的批处理作业。使用此策略的进程会尽量减少上下文切换，从而提高CPU缓存的局部性并减少开销，有利于提高整体系统吞吐量。

SCHED_IDLE：

空闲调度策略，应用于低优先级后台任务。这类任务只有在系统完全空闲时才会得到执行，不会影响其他任何进程的调度。

SCHED_DEADLINE：

截止期限（Deadline）调度策略，专为那些有严格截止时间约束的进程设计。此类进程需要在预设的截止时间内完成计算任务，调度器会确保进程在规定时间内获得足够的CPU资源来满足其执行需求。

> SCHED_NORMAL、SCHED_BATCH、SCHED_IDLE直接被映射到fair_sched_class  
> SCHED_RR、SCHED_FIFO与rt_schedule_class进行相关联