规律：全是书上图的延伸，有几率出作业题，最后一节会透题
# 往年期末
![[期末题1.png]]
**问题出在第二轮慢启动的结束阶段（即图中传输轮次 16~18 附近的处理逻辑）。**
18号到达应该1对应拥塞窗口为16，下一次到达20
- **现状分析：**
    
    - 在第13轮超时后，新的临界值（ssthresh）被设定为当前窗口的一半，即 $40 / 2 = 20$ KB。
        
    - 随后进入慢启动，窗口依次为：1 -> 2 -> 4 -> 8 -> **16**。
        
    - **关键点来了：** 当窗口为 **16 KB** 时，它是小于临界值 20 KB 的。
        
- **依据图中给出的简单算法：**
    
    - 图中左下角写着：“在临界值以下：拥塞窗口 = 2 × 拥塞窗口”。
        
    - 如果严格执行这个算法：下一次窗口应该变成 $16 \times 2 = 32$ KB。
        
- **导致的错误：**
    
    - 如果变成 32 KB，就直接**跳过并超过**了临界值 20 KB。
        
    - 这违背了慢启动的初衷（慢启动是为了快速逼近临界值，而不是冲过临界值）。TCP 规范要求在达到临界值后必须立即切换为线性增长（拥塞避免）。
        

### 2. 给出方案（修正逻辑）

你需要给慢启动的增长算法加一个 **“补丁”**（或者叫判别条件），防止指数增长“用力过猛”越过红线。

**修正后的算法描述：**

在发送方计算下一次拥塞窗口（cwnd）大小时，应增加一个判断：

- **如果 $cwnd \times 2 < ssthresh$（临界值）：**
    
    - 则 $cwnd = cwnd \times 2$（保持指数增长）。
        
- **如果 $cwnd \times 2 > ssthresh$：**
    
    - **则将 $cwnd$ 设定为 $ssthresh$。** （这一步非常关键，先把窗口“卡”在临界值上，而不是让它翻倍）。
        
    - 下一轮次开始，因为 $cwnd$ 已经等于 $ssthresh$，就会自然切换进入“拥塞避免”阶段（线性增长）。

![[期末终极复习题1.png]]
答：
![[8573261fc9b52c02b712426a54fadbdb.jpg]]

![[期末终极复习题2.png]]
答：![[e93b9f743197f90f2bd97a90820241f8.jpg]]
###  进阶变形：如果加上“传播时延”怎么办？

如果题目说：“两点之间距离很远，光速传播需要时间，每段链路的传播时延是 $t_{prop}$”，公式该怎么改？

这时候我们还是要盯着**“最后一个包”**。

#### 1. 还是分两段看

- **第一阶段：源主机（H1）狂发数据**
    
    - H1 把 1000 个包推送到第一段链路上。
        
    - 耗时：$1000 \times t_{trans}$（发送时延）。
        
    - **注意**：当 H1 发完第 1000 个包的瞬间，这个包其实还在第一段链路的起跑线上（刚离开网卡），它还没“飞”到 R1 呢。
        

#### 2. 第二阶段：最后一个包的“孤独旅程”

当 H1 刚把第 1000 个包吐出来（时间点 $1000 \times t_{trans}$），这个包需要经历什么才能到终点？

它需要走完**整条物理路径**，并且经过所有中间路由器的**转发**。

假设路径是 H1 $\rightarrow$ R1 $\rightarrow$ R2 $\rightarrow$ H2（3段链路，2个路由器）：

1. **链路1（H1-R1）**：
    
    - 它得先在光纤上飞一会儿：**$+ 1个传播时延$**。
        
    - （飞到了 R1）
        
2. **路由器 R1**：
    
    - R1 接收它、存储、再转发出去（发送时延）：**$+ 1个发送时延$**。
        
    - （此时它在 R1 的出口）
        
    - 它在链路2上飞一会儿：**$+ 1个传播时延$**。
        
    - （飞到了 R2）
        
3. **路由器 R2**：
    
    - R2 接收、转发（发送时延）：**$+ 1个发送时延$**。
        
    - （此时它在 R2 的出口）
        
    - 它在链路3上飞一会儿：**$+ 1个传播时延$**。
        
    - （飞到了 H2）
#### 3. 总结变形后的公式

$$T_{总} = \underbrace{N \times t_{trans}}_{\text{源主机发完所有包}} + \underbrace{(k-1) \times t_{trans}}_{\text{最后那个包在中间路由器的转发}} + \underbrace{k \times t_{prop}}_{\text{最后那个包在每一段路上的飞行时间}}$$

- $N$：分组数
    
- $t_{trans}$：发送时延
    
- $k$：链路数量（几段路）
    
- $t_{prop}$：单段链路的传播时延
    
**代入你的题目参数（假设有传播时延）：**

- $T = 1000t + 2t + 3 \times t_{prop}$
    
- 也就是：$1002t + 3t_{prop}$

![[期末终极复习题3.png]]


![[期末终极复习题3-1.png]]

![[期末终极复习题4.png]]


![[期末终极复习题5.png]]

![[期末终极复习题6.png]]

![[期末终极复习题7.png]]

作业题4-38
![[期末终极复习题8.png]]
![[{4206C369-E888-45F4-9A5A-231B5ADB1DC8}.png]]

![[期末终极复习题9.png]]

![[期末终极复习题1-1.png]]

P294
作业题11（a)
![[微信图片_20251219164521.jpg]]

作业5-30
![[期末终极复习题2-1.png]]

老干部：必考保命题
![[期末终极复习题3-2.png]]

P584分布式哈希表（高概率）
![[期末终极复习题4-1.png]]

老演员 udp和tcp必出一个
![[期末终极复习题4-2.png]]

可能会考的纯背题：
![[期末终极复习题.png]]

高概率可考
![[期末终极复习题-1.png]]

去年的背题：
隧道技术和服务质量定义及技术
![[期末终极复习题-2.png]]
![[微信图片_20251219164525.jpg]]
答：
![[{65B8FA05-5E84-4C51-A6E5-5F0C95DAC5BA}.png]]

![[期末终极复习题-3.png]]

去年题
![[期末终极复习题-4.png]]
![[{1AD55852-1993-4C91-B5E9-DFABB48F2DA5}.png]]
p312

![[期末终极复习题-5.png]]![[期末终极复习题-6.png]]

同上 逆向与汇集树
![[期末终极复习题-7.png]]

可能的简答题：
![[期末终极复习题-8.png]]

必背：
![[{E4896985-570E-4047-988B-13BDBDF32FFC}.png]]

去年考题：
P661
![[微信图片_20251219164518.jpg]]
![[期末复习习题.png]]

很经典的找出答案错哪里的题：
![[微信图片_20251219164509.jpg]]

没讲到的去年考题：
崩溃恢复
6-18
![[微信图片_20251219164513.jpg]]

P437低能窗口综合征解释

其他可能题目：
![[{E6AD72ED-CFD6-47E1-B0FE-B0EA67B54A95}.png]]

![[{E7E8FA7B-38CB-4C43-88DA-500510CF2AEE}.png]]

糊涂窗口综合征问题
![[{7E626363-A2A4-47EA-B700-46483E8EA404}.png]]

重点：漏桶和令牌桶
P314-316详解
![[{50109C2C-9293-47DC-9A93-B635DDA2A3EE}.png]]

计算CRC：p166

![[期末终极复习题-9.png]]

![[期末终极复习题-10.png]]![[期末终极复习题-11.png]]
